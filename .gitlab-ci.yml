# Define the stages of the pipeline
stages:
  - test
  - build
  - release

include:
  - component: $CI_SERVER_FQDN/hi-haibazo/devops/haibazo-devops-react-cpt/its-rct-ui@main
    inputs:
      sonarqube-token: $CI_WORKER_SONAR_TOKEN
      sonarqube-host-url: $CI_SERVICE_SONAR_HOST
      sonarqube-project-key: its-c_sandbox_its-sandbox-component_59f797f0-a4c1-4f59-aa42-3cec49d2c96a
      package-registry-auth-token: $CI_WORKER_REPOSITORY_TOKEN
