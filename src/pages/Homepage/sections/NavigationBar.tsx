import { Icons } from '@components/Atom/Icons';
import { Section } from '@components/Atom/Section';

export const NavigationBar = () => {
  return (
    <div>
      <Section px={52} py={19}>
        <Icons iconName="NayzakIcon"></Icons>
      </Section>
    </div>
  );
};
