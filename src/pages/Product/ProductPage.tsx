import { Section } from '@components/Atom/Section';

export const ProductPage = () => {
  return (
    <div style={{ width: '100%' }}>
      ProductPage you are here in my bile of heal
      <Section w={20} h={20} m="auto" style={{ backgroundColor: 'black' }}>
        <div style={{ backgroundColor: 'black' }}></div>
      </Section>
      oh no
    </div>
  );
};
